# ==============================================
# GitHub Bot Docker 环境配置
# ==============================================
# 复制此文件为 .env 并填写实际的配置值

# 应用安全密钥 (必须修改!)
SECRET_KEY=your-super-secret-key-change-in-production-please

# GitHub API 配置 (必填)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# 邮件通知配置 (可选，用于邮件推送)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_TLS=true
EMAIL_FROM=your-email@gmail.com

# Telegram 机器人配置 (可选)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# 企业微信配置 (可选)
WECHAT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your-key

# Slack 配置 (可选)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/slack/webhook

# ==============================================
# 配置说明:
# ==============================================
# 1. SECRET_KEY: 用于JWT加密，请生成一个强密码
# 2. GITHUB_TOKEN: GitHub Personal Access Token
#    - 获取地址: https://github.com/settings/tokens
#    - 需要权限: public_repo (或 repo)
# 3. 通知配置都是可选的，不填写对应的通知渠道将被禁用
# ==============================================